<% if flash[:success] %>
  <div class="flash-success brandon"><%= flash[:success] %></div>
<% end %>
<div class="content-shell"><img src="<%= image_url("glowbiotics/glowbiotics-teaser-landing.jpg") %>" /></div>
<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>




</div>

<div class="content-shell">
  <p class="ship brandon">We only ship in the United States.</p>
  <a class="policy brandon" href="/privacy-policy" target="_blank">Privacy Policy</a> | <a class="policy brandon" href="/rules" target="_blank">Offical Rules</a>
</div>
